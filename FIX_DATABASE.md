# 🔧 Виправлення проблеми з базою даних

## Проблема
Не можна зайти в систему - проблема з доступом до бази даних, де зберігаються користувачі.

## Рішення

### Крок 1: Перевірка підключення до MongoDB

Запустіть скрипт перевірки:
```bash
check-database.bat
```

Або вручну:
```bash
cd backend
node check-db.js
```

Це покаже:
- ✅ Чи підключена MongoDB
- ✅ Скільки користувачів в базі
- ✅ Список користувачів
- ✅ Створить дефолтного користувача якщо його немає

### Крок 2: Перевірка config.env

Відкрийте `backend/config.env` та перевірте:
```env
MONGODB_URI=mongodb+srv://darexuser:viktor22@cluster0.yaec2av.mongodb.net/test?retryWrites=true&w=majority&appName=Cluster0
```

Якщо URI неправильний, виправте його.

### Крок 3: Створення користувача вручну

Якщо користувачів немає, скрипт автоматично створить:
- **Логін:** `bugai`
- **Пароль:** `admin`
- **Роль:** `admin`

### Крок 4: Перезапуск Backend

Після перевірки перезапустіть backend:
```bash
start-backend.bat
```

## Діагностика

### Якщо помилка "MongoDB connection error":

1. **Перевірте чи доступний MongoDB сервер:**
   - Перевірте інтернет з'єднання
   - Перевірте чи сервер MongoDB працює

2. **Перевірте MONGODB_URI:**
   - Відкрийте `backend/config.env`
   - Переконайтеся що URI правильний
   - Перевірте credentials (логін/пароль)

3. **Перевірте firewall:**
   - MongoDB Atlas може блокувати з'єднання
   - Додайте ваш IP в whitelist MongoDB Atlas

### Якщо помилка "Користувач не знайдений":

1. **Запустіть check-database.bat** - він створить дефолтного користувача

2. **Або створіть користувача вручну:**
   - Використовуйте MongoDB Compass або інший клієнт
   - Або запустіть скрипт `check-db.js`

## Очікуваний результат

Після виправлення:
- ✅ MongoDB підключена
- ✅ Користувачі знаходяться в базі
- ✅ Можна увійти з логіном `bugai` / паролем `admin`
- ✅ Backend показує: `✅ MongoDB connected successfully`

## Логування

Backend тепер показує детальну інформацію:
- Стан підключення до MongoDB
- Кількість користувачів
- Деталі спроб входу
- Помилки з деталями

Перевірте консоль backend для діагностики.
