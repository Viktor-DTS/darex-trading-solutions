# 🚀 Швидкий старт - NewServiceGidra

## Автоматичне встановлення та запуск

### Варіант 1: Встановити все одразу (рекомендовано)

Просто запустіть:
```bash
install-all.bat
```

Це встановить всі залежності для backend та frontend.

### Варіант 2: Встановити окремо

**Backend:**
```bash
install-backend.bat
```

**Frontend:**
```bash
install-frontend.bat
```

## Запуск проекту

### Варіант 1: Запустити все одразу (рекомендовано)

Просто запустіть:
```bash
start-all.bat
```

Це запустить:
- Backend на `http://localhost:3001`
- Frontend на `http://localhost:5173`

### Варіант 2: Запустити окремо

**Backend:**
```bash
start-backend.bat
```

**Frontend:**
```bash
start-frontend.bat
```

## Відкрийте браузер

Після запуску перейдіть на:
```
http://localhost:5173
```

## Налаштування

### MongoDB Connection

Файл `backend\config.env` вже створено з налаштуваннями за замовчуванням.

Якщо потрібно змінити, відредагуйте `backend\config.env`:
```env
MONGODB_URI=your_mongodb_connection_string
JWT_SECRET=your-secret-key
PORT=3001
```

## Структура файлів

```
NewServiceGidra/
├── install-all.bat          # Встановити всі залежності
├── install-backend.bat      # Встановити backend
├── install-frontend.bat     # Встановити frontend
├── start-all.bat            # Запустити все
├── start-backend.bat        # Запустити backend
├── start-frontend.bat       # Запустити frontend
├── backend/
│   ├── config.env          # Конфігурація (вже створено)
│   └── ...
└── frontend/
    └── ...
```

## Що далі?

1. ✅ Встановіть залежності: `install-all.bat`
2. ✅ Запустіть проект: `start-all.bat`
3. ✅ Відкрийте браузер: `http://localhost:5173`
4. 🔄 Додайте всю логіку з оригінального проекту
5. 🔄 Налаштуйте авторизацію (якщо потрібно)
6. 🔄 Додайте інші панелі

## Підтримка

Якщо виникли проблеми:
1. Перевірте, чи встановлений Node.js (версія 16+)
2. Перевірте, чи запущений MongoDB
3. Перевірте правильність MONGODB_URI в `backend\config.env`
4. Перевірте консоль браузера та сервера на помилки

## Команди вручну (якщо потрібно)

### Backend
```bash
cd backend
npm install
npm start
```

### Frontend
```bash
cd frontend
npm install
npm run dev
```
