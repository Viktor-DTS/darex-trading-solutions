# 🚀 Локальна розробка DTS Service

## 📋 Швидкий старт

### 1. Запуск Backend (Термінал 1)
```bash
# Запустити скрипт
start-local.bat

# Або вручну:
cd backend
copy config.local.env .env
npm install
npm start
```

### 2. Запуск Frontend (Термінал 2)
```bash
# Запустити скрипт
start-frontend.bat

# Або вручну:
cd frontend
npm install
npm run dev
```

## 🌐 Доступні URL

- **Frontend:** http://localhost:3000
- **Backend API:** http://localhost:3001/api
- **Тестовий endpoint:** http://localhost:3001/api/test-invoice

## 🔧 Переваги локальної розробки

✅ **Миттєве тестування** - зміни відображаються одразу  
✅ **Повний доступ до бази** - використовуємо продакшн дані  
✅ **Детальне логування** - бачимо всі помилки в консолі  
✅ **Швидка діагностика** - не чекаємо розгортання  
✅ **Безпечне тестування** - зміни не впливають на продакшн  

## 🛠️ Налаштування

### Backend конфігурація
- **База даних:** MongoDB Atlas (продакшн)
- **Порт:** 3001
- **CORS:** localhost:3000, localhost:3001

### Frontend конфігурація
- **API URL:** http://localhost:3001/api (автоматично)
- **Порт:** 3000
- **Hot reload:** увімкнено

## 🐛 Діагностика проблем

### Перевірка підключення до бази
```bash
# Тестовий endpoint
curl http://localhost:3001/api/test-invoice
```

### Логи backend
Всі логи відображаються в терміналі backend сервера

### Логи frontend
Відкрийте Developer Tools (F12) → Console

## 📝 Робочий процес

1. **Розробка:** Вносимо зміни локально
2. **Тестування:** Перевіряємо на localhost
3. **Діагностика:** Аналізуємо логи
4. **Виправлення:** Вносимо правки
5. **Деплой:** Коли все працює → комітимо на продакшн

## ⚠️ Важливо

- **НЕ ЗАКРИВАЙТЕ** термінали під час розробки
- **Backend повинен запускатися першим**
- **Перевіряйте логи** при помилках
- **Тестуйте на продакшн базі** для точності
